#!/bin/bash

ASSEMBLE_SCRIPT="/usr/libexec/s2i/assemble"

export PATH=$PATH:/opt/app-root/node_modules/.bin

cd ..
npm install yarn
cd src

node -v
npm -v
yarn -v


pushd /tmp/src || exit 1

yarn install 

popd

if [ -x "$ASSEMBLE_SCRIPT" ]; then
	$ASSEMBLE_SCRIPT
fi
